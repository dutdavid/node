//The for-in loop is designed to loop over an object and its properties.
//We loop over the properties inside the object.
const person={"first":"James","last":"Muriuki"}

for(let prop in person){
  console.log(prop)
}

// first
// last
